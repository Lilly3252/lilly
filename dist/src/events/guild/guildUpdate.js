export const name = 'guildUpdate';
export const once = false;
export const run = async (oldGuild, newGuild) => {
    console.log(`${oldGuild.name} updated to ${newGuild.name}`);
};
//# sourceMappingURL=guildUpdate.js.map